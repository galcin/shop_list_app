/// Annotation to mark widgets that can be previewed in the IDE.
/// This helps developers identify which widgets are ready for live preview
/// during development.
class Preview {
  const Preview();
}

const preview = Preview();
