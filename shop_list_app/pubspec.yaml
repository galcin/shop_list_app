name: shop_list_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0" # <-- minimum for latest mockito

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.15.0
  path_provider: ^2.0.14
  sqlite3_flutter_libs: ^0.5.0  
  drift_flutter: ^0.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.6.3
  drift_dev: ^2.14.0
  build_runner: ^2.4.8

flutter:
  uses-material-design: true